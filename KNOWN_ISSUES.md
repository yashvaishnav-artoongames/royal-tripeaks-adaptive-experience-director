Moved. The current issue list is `docs/KNOWN_ISSUES.md`.

This root copy was byte-identical until 1.1.0 and has since drifted — ISSUE-006 to
ISSUE-009 exist only in `docs/`. Kept as a pointer rather than deleted so nothing
that links here breaks.
