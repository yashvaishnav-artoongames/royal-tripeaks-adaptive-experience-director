Moved. The current handoff is `docs/SESSION_HANDOFF.md`.

This root copy went stale and a fresh session that read it would have been working
from 1.0.1. Kept as a pointer rather than deleted so nothing that links here breaks.
